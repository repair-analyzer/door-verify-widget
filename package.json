{
  "name": "garagecam-quotes",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone multi-tenant embeddable widget for secure garage door quote intake.",
  "type": "commonjs",
  "scripts": {
    "build": "mkdir -p dist && cp src/garagecam-quotes.js dist/garagecam-quotes.js && cp config/infrastructure.json dist/infrastructure.json",
    "lint": "node --check src/garagecam-quotes.js && node --check tests/garagecam-quotes.test.js && node -e \"JSON.parse(require('node:fs').readFileSync('config/infrastructure.json', 'utf8'))\"",
    "test": "node --test tests/*.test.js"
  }
}
